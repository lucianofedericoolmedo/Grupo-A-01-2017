language: java
sudo: required
jdk:
  - oraclejdk8
  
deploy:
  provider: heroku
  api_key:
    secure: "YOUR ENCRYPTED API KEY"
  
install:
- cd desapp-groupA-backend && mvn install -Dmaven.compiler.target=1.8 -Dmaven.compiler.source=1.8 
